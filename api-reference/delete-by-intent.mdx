---
title: 'Delete by Intent'
description: 'API reference for the delete by intent endpoint'
openapi: "POST /delete_by_intent"
---

The `/delete_by_intent` endpoint allows you to remove knowledge entries that match a specific intent.

## Base URL

```bash
POST https://api.osmosis.ai/delete_by_intent
```

## Authentication

<RequestHeader>
  <HeaderParameter name="Content-Type" required type="string">
    application/json
  </HeaderParameter>
  <HeaderParameter name="Authorization" required type="string">
    Bearer YOUR_API_KEY
  </HeaderParameter>
  <HeaderParameter name="x-feature-store-id" required type="string">
    YOUR_FEATURE_STORE_ID
  </HeaderParameter>
</RequestHeader>

<Info>
  The <code>x-feature-store-id</code> header determines which feature store (collection) the model will use for this request. This allows you to manage multiple stores/collections within your workspace.
</Info>

## Query Parameters

<ParamField query="intent" type="string" required>
  The intent to match against
</ParamField>

<ParamField query="similarity_threshold" type="number" default="0.5">
  Optional similarity threshold for matching documents
</ParamField>

## Example Requests

<CodeGroup>

```bash cURL
curl -X POST 'https://api.osmosis.ai/delete_by_intent?intent=process%20refund&similarity_threshold=0.7' \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "x-feature-store-id: YOUR_FEATURE_STORE_ID"
```

```typescript TypeScript
import axios from 'axios';

async function deleteByIntent() {
  const apiKey = process.env.OSMOSIS_API_KEY;
  const featureStoreId = process.env.FEATURE_STORE_ID;
  
  const response = await axios.post('https://api.osmosis.ai/delete_by_intent', null, {
    params: {
      intent: 'process refund',
      similarity_threshold: 0.7
    },
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${apiKey}`,
      'x-feature-store-id': featureStoreId
    }
  });
  
  return response.data;
}
```

```python Python
import os
import requests

def delete_by_intent():
    api_key = os.getenv('OSMOSIS_API_KEY')
    feature_store_id = os.getenv('FEATURE_STORE_ID')
    
    params = {
        "intent": "process refund",
        "similarity_threshold": 0.7
    }
    
    response = requests.post(
        "https://api.osmosis.ai/delete_by_intent",
        params=params,
        headers={
            "Content-Type": "application/json",
            "Authorization": f"Bearer {api_key}",
            "x-feature-store-id": feature_store_id
        }
    )
    
    return response.json()
```

```go Go
package main

import (
    "encoding/json"
    "net/http"
    "net/url"
    "os"
)

func deleteByIntent() ([]byte, error) {
    apiKey := os.Getenv("OSMOSIS_API_KEY")
    featureStoreId := os.Getenv("FEATURE_STORE_ID")
    baseURL := "https://api.osmosis.ai/delete_by_intent"
    
    params := url.Values{}
    params.Add("intent", "process refund")
    params.Add("similarity_threshold", "0.7")
    
    req, err := http.NewRequest("POST", baseURL+"?"+params.Encode(), nil)
    if err != nil {
        return nil, err
    }

    req.Header.Set("Content-Type", "application/json")
    req.Header.Set("Authorization", "Bearer "+apiKey)
    req.Header.Set("x-feature-store-id", featureStoreId)

    client := &http.Client{}
    resp, err := client.Do(req)
    if err != nil {
        return nil, err
    }
    defer resp.Body.Close()

    return json.Marshal(resp)
}
```

</CodeGroup>

## Response

### Success Response

<ResponseField name="deleted_count" type="number" required>
  Number of documents deleted
</ResponseField>

<ResponseField name="status" type="string" required>
  Operation status message
</ResponseField>

Example success response:

```json
{
  "deleted_count": 5,
  "status": "Successfully deleted matching documents"
}
```

### Error Response

<ResponseField name="detail" type="array" required>
  Array of validation errors
</ResponseField>

Example error response:

```json
{
  "detail": [
    {
      "loc": ["query"],
      "msg": "field required",
      "type": "value_error.missing"
    }
  ]
}
```

## Status Codes

<ResponseStatus code={200} status="OK">
  Documents deleted successfully
</ResponseStatus>

<ResponseStatus code={422} status="Unprocessable Entity">
  Invalid request (validation error)
</ResponseStatus>

## Best Practices

1. **Similarity Threshold**
   - Start with the default threshold of 0.5
   - Increase for more precise matching
   - Decrease for broader matching
   - Test with small deletions first

2. **Intent Formatting**
   - Use clear, specific intents
   - Keep intents concise
